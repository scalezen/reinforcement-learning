#include "SplitMix64.h"

// The real known-answer test lives in test_splitmix64.cpp (gtest).
// This file just satisfies the "splitmix64.cpp builds to an executable"
// convention used elsewhere in this repo; running it directly isn't meaningful.
int main() {
    return 1;
}
